# Smengo — Дорожная карта «Платформа»

> **Живой документ.** Claude ведёт его по ходу работ: при старте пункта ставит 🔄 и дату,
> при завершении — ✅ и запись в «Журнале выполнения» внизу. Изменения скоупа тоже фиксируются в журнале.
> Решение основателя от 2026-06-12: строим **все 10 пунктов меню «Платформа»**, границы MVP из CLAUDE.md расширены.

- **Создан:** 2026-06-12
- **Источник:** анализ кода, живой БД (Supabase `mryjttyuerhayoehoaoq`) и меню `components/marketing/platform-groups.tsx`
- **Статусы:** ⬜ не начат · 🔄 в работе · ✅ готов · ⏸ отложен

---

## Карта меню → этапы

| Пункт меню «Платформа» | Маркетинг-страница | Этап | Статус |
|---|---|---|---|
| График смен | ✅ готова (с демо) | 1 | ✅ |
| Быстрый интерфейс | заглушка | 1 (свойство грида) | ✅ |
| Хранилище данных сотрудников | ✅ готова (с демо) | 2 | ⬜ |
| HR Dashboard | ✅ готова (с демо) | 3 | ⬜ |
| HR Management | ✅ готова (с демо) | 4 | ⬜ |
| Адаптация сотрудников | ✅ готова (с демо) | 4 | ⬜ |
| Telegram-бот: чекины и оповещения | заглушка | 5 | ⬜ |
| Замены смен | заглушка | 6 | ⬜ |
| AI-помощник | заглушка | 7 | ⬜ |
| Мотивация команды | заглушка | 8 | ⬜ |

**Логика порядка:** всё в платформе питается от двух источников — карточек сотрудников и записей графика.
Пока их нет, дашборду нечего считать, боту не о чем оповещать, AI нечего заполнять. Сначала ядро (этапы 0–1),
потом данные и HR-слой (2–4), потом интеграции и автоматика поверх (5–8).

---

## Текущее состояние (зафиксировано 2026-06-12)

**Уже работает:** auth (login/register/reset), онбординг организации, инвайты по токену,
скелет биллинга Paddle (webhook + actions), i18n ru/uk/en, лендинг с прайсингом и 5 продуктовыми страницами,
Sentry, middleware локалей.

**БД (12 таблиц, RLS захарден миграциями 2026-06-11):** organizations, memberships, profiles, departments,
employees *(уже с полями карточки: phone, telegram, email, birth_date, hired_on, note)*, schedule_entries
*(уже со start_time/end_time)*, status_types *(уже со временем смен)*, alert_configs, invitations,
subscriptions, webhook_events, manager_departments. Данных нет: 0 сотрудников, 0 записей графика.

**Не существует в продукте:** ни один пункт меню. `app/(app)/dashboard/page.tsx` — заглушка «Schedule grid coming soon».

---

## Этап 0 — Фундамент приложения

- **Статус:** ✅ готов (2026-06-12)
- **Закрывает:** ничего в меню напрямую; разблокирует всё остальное
- **Оценка:** 2–3 дня → фактически 1 сессия
- **Новые таблицы:** нет

Задачи:
- [x] App shell залогиненной зоны: layout с сайдбаром-навигацией (`app/(app)/(shell)/layout.tsx`, `components/app/sidebar.tsx`); онбординг вне shell
- [x] Гард ролей: `lib/auth/context.ts` → `getAppContext()` (user + активная org + роль + isReadOnly), роль видна в сайдбаре
- [x] Переключатель активной организации: `switchOrgAction` + select в сайдбаре (показывается при >1 организации)
- [x] Проверен сидинг системных статусов на живой БД: 5 статусов (work/vacation/sick/dayoff/late) с лейблами ru/uk/en на месте
- [x] Снят фейковый social proof в ru/uk/en: «400+ компаний» → «ранний доступ», «данные пилотных команд» → «модельный сценарий», бэнд-стат → «14 дней триала»
- [x] Каркас страниц: /schedule, /employees, /dashboard — пустые состояния с CTA (`components/app/empty-state.tsx`)

Definition of Done: ✅ проверено живым E2E-прогоном 2026-06-12 (см. журнал).

> ## 🔒 SECURITY REVIEW ЭТАПА — ✅ ВЫПОЛНЕН 2026-06-12
> ### Полный аудит проекта Sentry-скиллом: 0 эксплуатируемых находок. Добавлены security-заголовки и единое экранирование JSON-LD.

---

## Этап 1 — График смен (ядро) 🎯

- **Статус:** ✅ готов (2026-06-12, слит в main merge-коммитом d80a1c0)
- **Закрывает в меню:** «График смен», «Быстрый интерфейс»
- **Зависит от:** этап 0
- **Оценка:** 2–3 недели → фактически 1 сессия (subagent-driven, 22 задачи плана)

Задачи:
- [x] CRUD отделов: server actions (`lib/actions/departments.ts`), Zod-схемы, модалки
- [x] CRUD сотрудников: `lib/actions/employees.ts`, модалка, мягкое удаление через `deleted_at`
- [x] Drag-and-drop порядок — атомарный RPC `reorder_employees` с проверкой принадлежности
- [x] Грид: `components/schedule/` — TanStack Virtual, группы отделов, 3 режима ячеек по дизайну демо
- [x] Загрузка месяца → `Map<employeeId, Map<dateISO, entry>>`, TanStack Query (+пагинация мимо PostgREST max_rows!)
- [x] Редактирование ячейки: optimistic upsert, пикер статусов, пресеты Утро/Вечер/Ночь, время
- [x] Кастомные статусы: менеджер статусов (палитра демо), `counts_as_present`
- [x] Покрытие: строка под шапкой, пороги per-отдел (`alert_configs`), подсветка дефицита
- [x] Фильтры: отдел, поиск (дебаунс), режимы — shallow URL (шарящиеся ссылки без RSC-рефетча)
- [x] Вкладка «Сотрудники»: карточки/список, оверлей с мини-календарём (паритет с демо)
- [x] Quick start пустой организации + баннер первого сотрудника
- [x] Лимиты плана — server-side в `createEmployeeAction` (fail-closed)
- [x] «Быстрый интерфейс»: виртуализация проверена на 300×30 (17 строк в DOM из 303)
- [ ] ~~Экспорт месяца в PDF и PNG~~ → вынесен в мини-этап 1.5 (решение в спеке 2026-06-12)

Definition of Done: ✅ выполнен, проверен живым E2E + перф-прогоном на 300 сотрудников (см. журнал).

> ## 🔒 SECURITY REVIEW ЭТАПА — ✅ ВЫПОЛНЕН 2026-06-12
> ### Sentry-скилл по всему новому коду (actions, RLS, RPC, клиент): 0 эксплуатируемых находок. RLS-интеграционные тесты: TESTS_PASSED (9 сценариев на живой БД).

---

## Этап 1.5 — Экспорт графика в PDF/PNG (мини-этап)

- **Статус:** 🔄 в работе (старт 2026-06-12)
- **Закрывает:** обещание экспорта со страницы «График смен» (вынесено из этапа 1 решением в спеке)
- **Зависит от:** этап 1
- **Скоуп:** определяется brainstorm → спека → план по методологии superpowers; задачи появятся здесь после спеки

> ## 🔒 ОБЯЗАТЕЛЬНО ПОСЛЕ ЭТАПА: SECURITY REVIEW
> ### Прогнать `/security-review`: генерация файлов (инъекции в контент PDF, утечка данных чужих org в экспорт), эндпоинты/actions экспорта. Находки — в журнал.

---

## Этап 2 — Хранилище данных сотрудников

- **Статус:** ⬜ не начат
- **Закрывает в меню:** «Хранилище данных сотрудников»
- **Зависит от:** этап 1 (CRUD сотрудников)
- **Оценка:** 1–1,5 недели
- **Новые таблицы:** `employee_documents`, `audit_log` (+ Storage bucket `employee-docs` с RLS)

Обещания страницы /platform/employee-database, которые надо выполнить: контакты и Telegram, документы со сроками,
даты и стаж, роли и доступы, история изменений, связь с графиком.

Задачи:
- [ ] Страница-карточка `app/(app)/employees/[id]`: контакты, telegram, email, даты, стаж (поля уже в БД)
- [ ] Миграция `employee_documents` (id, org_id, employee_id, title, file_path, expires_on, uploaded_by, created_at) + RLS
- [ ] Storage bucket `employee-docs` + политики доступа по org_id и ролям
- [ ] Загрузка/просмотр документов, бейджи «истекает через N дней»
- [ ] Миграция `audit_log` (id, org_id, actor_id, entity, entity_id, action, diff jsonb, created_at) + триггеры на employees / schedule_entries
- [ ] Вкладка «История изменений» в карточке
- [ ] Живой поиск по сотрудникам (имя, должность, телефон, telegram)
- [ ] Связь с графиком: из карточки — месяц сотрудника, из грида — клик по имени → карточка

Definition of Done: любой сотрудник находится поиском «за две секунды» (обещание лендинга), документы со сроками живут
в карточке, история изменений пишется автоматически.

> ## 🔒 ОБЯЗАТЕЛЬНО ПОСЛЕ ЭТАПА: SECURITY REVIEW
> ### Прогнать `/security-review`: Storage-политики bucket'а документов (path traversal, доступ по org_id), audit_log (нет ли утечки PII), поиск. Находки — в журнал.

---

## Этап 3 — HR Dashboard

- **Статус:** ⬜ не начат
- **Закрывает в меню:** «HR Dashboard»
- **Зависит от:** этапы 1–2 (нужны данные грида и карточек)
- **Оценка:** ~1 неделя
- **Новые таблицы:** `hr_tasks`; события календаря — генерация из дат карточек (ДР, годовщины) + отпуска из грида

Обещания страницы /platform/hr-dashboard: метрики обновляются сами, часы и нагрузка команды, календарь команды и события,
отпуска и больничные, задачи HR.

Задачи:
- [ ] Переделать `app/(app)/dashboard/page.tsx` из заглушки в реальный дашборд
- [ ] Агрегатные запросы: headcount, часы по периодам (из start/end time записей), распределение по типам занятости
- [ ] Виджет «Отпуска и больничные» (статусы с `counts_as_present = false` на ближайшие недели)
- [ ] Календарь команды: дни рождения, годовщины найма, события
- [ ] Миграция `hr_tasks` (id, org_id, title, due_on, assignee_id, done_at, created_by) + RLS, виджет задач
- [ ] График «Часы команды» с переключением периода (recharts)
- [ ] Скоуп по ролям: manager видит только свои отделы (`visible_department_ids()`)

Definition of Done: утренняя сводка по команде без ручного сбора — обещание «вся команда в цифрах на одном экране» выполняется.

> ## 🔒 ОБЯЗАТЕЛЬНО ПОСЛЕ ЭТАПА: SECURITY REVIEW
> ### Прогнать `/security-review`: агрегатные запросы (скоуп manager'а через visible_department_ids), hr_tasks RLS, утечки данных между отделами. Находки — в журнал.

---

## Этап 4 — HR Management + Адаптация сотрудников

- **Статус:** ⬜ не начат
- **Закрывает в меню:** «HR Management», «Адаптация сотрудников»
- **Зависит от:** этап 2
- **Оценка:** ~2 недели
- **Новые таблицы:** `candidates`, `onboarding_templates`, `onboarding_tracks`, `onboarding_tasks`

Делаются вместе — это одна цепочка, как и продают страницы: кандидат → оффер → трек адаптации → сотрудник → в график.

Задачи:
- [ ] Миграция `candidates` (id, org_id, full_name, contacts jsonb, position, stage, source, note, sort_order, created_at) + RLS
- [ ] Kanban-пайплайн найма: этапы (отклик → интервью → оффер → принят), drag-and-drop карточек
- [ ] Миграция треков: `onboarding_templates` (шаблон на должность), `onboarding_tracks` (трек конкретного новичка), `onboarding_tasks` (этапы «до выхода / день 1 / неделя 1 / месяц», ответственный, дедлайн, done_at) + RLS
- [ ] UI трека: прогресс по этапам, чек-листы, роли «HR / наставник / новичок» видят своё
- [ ] Конвертации: кандидат «принят» → создаётся employee + трек адаптации; трек завершён → сотрудник в графике
- [ ] Уведомления ответственным о просроченных шагах (email через Resend; в этапе 5 добавится Telegram)

Definition of Done: кандидата можно провести от отклика до первой смены в графике, не выходя из Smengo.

> ## 🔒 ОБЯЗАТЕЛЬНО ПОСЛЕ ЭТАПА: SECURITY REVIEW
> ### Прогнать `/security-review`: RLS кандидатов и треков (PII соискателей!), конвертация кандидат → сотрудник, email-уведомления (инъекции в шаблоны). Находки — в журнал.

---

## Этап 5 — Telegram-бот: чекины и оповещения

- **Статус:** ⬜ не начат
- **Закрывает в меню:** «Telegram-бот: чекины и оповещения»
- **Зависит от:** этап 1 (живые смены со временем), `employees.telegram` уже в БД
- **Оценка:** 1,5–2 недели
- **Новые таблицы:** `shift_checkins`, `telegram_accounts` (привязка chat_id к employee)

Задачи:
- [ ] Бот на grammY: route handler `app/api/telegram/webhook` (проверка secret token), env `TELEGRAM_BOT_TOKEN`
- [ ] Привязка сотрудника: deep-link код из карточки → /start в боте → запись в `telegram_accounts`
- [ ] Миграция `shift_checkins` (id, org_id, employee_id, entry_id, type in/out, checked_at) + RLS
- [ ] Чекин на смену из бота, статус чекинов виден в гриде
- [ ] Оповещения сотруднику: «завтра смена в 9:00», изменение смены, назначение замены
- [ ] Алерт менеджеру: «смена началась, N человек не зачекинились» (Vercel cron)
- [ ] Настройки оповещений на уровне организации

Definition of Done: сотрудник без доступа к приложению получает свой график и чекинится из Telegram; менеджер видит опоздания в момент, а не вечером.

> ## 🔒 ОБЯЗАТЕЛЬНО ПОСЛЕ ЭТАПА: SECURITY REVIEW — КРИТИЧНЫЙ ЭТАП
> ### Прогнать `/security-review` с особым вниманием: вебхук бота (проверка secret token!), привязка аккаунтов (нельзя привязаться к чужому сотруднику), чекины (подделка), cron-эндпоинты. Это первая внешняя поверхность атаки после Paddle. Находки — в журнал.

---

## Этап 6 — Замены смен

- **Статус:** ⬜ не начат
- **Закрывает в меню:** «Замены смен»
- **Зависит от:** этапы 1 и 5
- **Оценка:** ~1 неделя
- **Новые таблицы:** `shift_swap_requests`

Задачи:
- [ ] Миграция `shift_swap_requests` (id, org_id, entry_id, from_employee_id, to_employee_id, status, comment, decided_by, decided_at, created_at) + RLS
- [ ] Запрос замены из бота (и из приложения)
- [ ] Экран согласования у менеджера + push в Telegram
- [ ] Применение замены: транзакционный RPC (SECURITY DEFINER) — перенос записи графика, запись в audit_log
- [ ] Уведомления обеим сторонам о решении

Definition of Done: «не могу выйти в субботу» решается в боте за минуты и сам обновляет грид.

> ## 🔒 ОБЯЗАТЕЛЬНО ПОСЛЕ ЭТАПА: SECURITY REVIEW
> ### Прогнать `/security-review`: RPC применения замены (авторизация обеих сторон, race conditions при двойном согласовании), статусы заявок. Находки — в журнал.

---

## Этап 7 — AI-помощник

- **Статус:** ⬜ не начат
- **Закрывает в меню:** «AI-помощник» (+ обещание «Доверьте AI» на странице грида)
- **Зависит от:** этапы 1–3 (богатые данные)
- **Оценка:** 1–2 недели
- **Новые таблицы:** `ai_runs` (журнал и лимиты запусков)

Задачи:
- [ ] Слой Claude API в server actions (ключ только на сервере; перед стартом — свериться с актуальным справочником моделей/цен)
- [ ] Автозаполнение месяца: по правилам покрытия (alert_configs), пожеланиям и истории — с предпросмотром и ручным подтверждением до записи
- [ ] Q&A по графику: «кто работает в субботу?», «у кого переработки в мае?»
- [ ] Лимиты запусков по тарифному плану, журнал `ai_runs`
- [ ] Обработка отказов/ошибок API, fallback в ручной режим

Definition of Done: менеджер получает черновик месяца одной кнопкой и применяет его осознанно (diff-предпросмотр).

> ## 🔒 ОБЯЗАТЕЛЬНО ПОСЛЕ ЭТАПА: SECURITY REVIEW
> ### Прогнать `/security-review`: prompt injection через данные графика/имена сотрудников, утечка данных чужих организаций в контекст AI, лимиты `ai_runs`, ключ API только на сервере. Находки — в журнал.

---

## Этап 8 — Мотивация команды

- **Статус:** ⬜ не начат
- **Закрывает в меню:** «Мотивация команды»
- **Зависит от:** этап 5 (данные чекинов)
- **Оценка:** ~1 неделя, **скоуп определяем по живым пользователям**
- **Новые таблицы:** `kudos` (благодарности); стрики — деривmemo из `shift_checkins`

Задачи (черновик, уточнить перед стартом этапа):
- [ ] Стрики чекинов без опозданий (виджет в дашборде и в боте)
- [ ] Благодарности/признания между коллегами
- [ ] Сводка месяца для команды

Definition of Done: определяется при старте этапа — к этому моменту будут данные о том, чем реально пользуются.

> ## 🔒 ОБЯЗАТЕЛЬНО ПОСЛЕ ЭТАПА: SECURITY REVIEW
> ### Прогнать `/security-review`: kudos/стрики (видимость между отделами, накрутка через подделку чекинов). Финальный полный аудит всей платформы перед публичным запуском. Находки — в журнал.

---

## Сквозные правила (чек-лист каждого этапа)

- [ ] Каждая новая таблица: колонка `org_id` + RLS-политика **до** первого запроса
- [ ] Миграции — файлами в `supabase/migrations/`, применение через `supabase db push`
- [ ] После изменений схемы: `supabase gen types typescript --linked > supabase/types.ts` (+ алиасы UserRole/SubscriptionStatus/PlanTier)
- [ ] Интеграционные тесты БД (паттерн `do $$ ... $$` + `set_config('request.jwt.claims', ...)` + откат через `raise exception 'TESTS_PASSED'`)
- [ ] i18n: все строки через next-intl, ключи в ru/uk/en одновременно
- [ ] Лимиты плана — финальная проверка на сервере, UI-проверка только для UX
- [ ] Никакого service_role в коде, доступном из браузера; service_role-код вручную фильтрует org_id
- [ ] PostHog-события на ключевые действия этапа; ошибки — в Sentry
- [ ] Дизайн-токены вместо магических значений
- [ ] **Тестирование после каждого этапа:** typecheck + vitest + production build + ручная проверка Definition of Done в превью; результаты — в журнал
- [ ] **🔒 SECURITY REVIEW после каждого этапа:** прогон `/security-review` (Sentry-скилл) по новому коду этапа — блоки-напоминания стоят в конце каждого этапа выше; этап не считается закрытым без него

---

## Журнал выполнения

| Дата | Событие |
|---|---|
| 2026-06-12 | Дорожная карта создана. Решение основателя: строим все 10 пунктов меню «Платформа», границы MVP расширены. Зафиксировано текущее состояние: продукт-зона — заглушка, БД готова под этапы 0–1, данных нет. |
| 2026-06-12 | **Регистрация переработана** (по образцу 7shifts, решение основателя): имя+фамилия, название компании, «откуда узнали» (255 зн., опционально), чекбокс условий, value-props «14 дней бесплатно / отмена в любой момент», «карта не нужна». Миграция: `organizations.acquisition_source` + новая сигнатура `create_organization`. Онбординг стал шагом добора данных: для email-регистраций всё префиллится из user_metadata (источник/условия скрыты), для Google OAuth — имя парсится из full_name, показываются компания, источник и чекбокс условий; серверная проверка условий в `createOrgAction`. Профиль получает full_name. Тесты: tsc чисто, vitest 75/75, build ok. E2E: email-флоу (регистрация → подтверждение → префилл → организация с acquisition_source в БД) и симуляция Google-флоу (split имени, добор полей, отказ без галочки, успех с галочкой) — всё прошло; тестовые данные прогона удалены из БД. TODO-чип: страницы Terms/Privacy (чекбокс пока без ссылок). |
| 2026-06-12 | **Форма регистрации уплотнена до одного экрана** (как 7shifts): убран дублирующий подзаголовок, компактнее паддинги/ритм карточки и auth-layout, счётчик символов показывается только при вводе, инпуты py-1.5. Проверено: 1440×900 и 1280×800 — вся форма с кнопкой и футером видна без скролла в обеих темах; мобильная 375px без горизонтального переполнения. Онбординг-форма уплотнена так же. tsc чисто, vitest 75/75. |
| 2026-06-12 | **Выход из онбординг-ловушки**: после OAuth-логина пользователь без организации не мог сменить аккаунт — онбординг был тупиком. Под картой добавлена строка «Вы вошли как … · Не тот аккаунт? Выйти» (i18n ru/uk/en), logout → /login, /register снова доступен. Проверено E2E на пользователе без организации; tsc чисто, vitest 75/75. |
| 2026-06-12 | **Этап 1 завершён (ветка `feat/schedule-grid`, 41 коммит, +6,4k строк).** Superpowers-цикл: brainstorm → спека → план (22 задачи) → subagent-driven выполнение (свежий субагент на задачу + spec/quality-ревью каждой). Рабочий грид: 3 режима ячеек, редактор с пресетами, optimistic-апдейты, покрытие с порогами, фильтры/поиск в shallow-URL, dnd-порядок (атомарный RPC), вкладка «Сотрудники» (карточки/список/оверлей), кастомные статусы, quick start, лимиты плана. Найдено и исправлено в процессе: RLS-баг INSERT RETURNING на departments (политика dept_select чинена миграцией), обрезка месяца PostgREST max_rows=1000 на 300 сотрудниках (пагинация в фетчере), нарушение rules-of-hooks в settings-панелях, lookup системного статуса. Верификация: tsc/lint/build чистые, vitest 118/118, RLS-тесты TESTS_PASSED (9 сценариев), живой E2E (отдел→сотрудники→смены→покрытие), перф 300×30 (виртуализация: 17 строк в DOM). Security review: 0 находок. Полиш-бэклог: sticky заголовка группы при гориз. скролле, код статуса в чипах редактора, локализация aria пустых ячеек, экспорт PDF/PNG (мини-этап 1.5). |
| 2026-06-12 | **Security review встроен в процесс**: после каждого этапа (1–8) в роадмап добавлены крупные блоки-напоминания «🔒 ОБЯЗАТЕЛЬНО ПОСЛЕ ЭТАПА: SECURITY REVIEW» с фокусом проверки под конкретный этап (этап 5 — бот — помечен критичным). Полный аудит проекта выполнен сегодня Sentry-скиллом: 0 эксплуатируемых находок; добавлены security-заголовки (next.config.ts) и единый экранированный JsonLd-компонент для всех 8 вставок структурированных данных. |
| 2026-06-12 | **Этап 1 слит в main** (merge-коммит `d80a1c0`, --no-ff, 45 коммитов, +6,4k строк; ветка `feat/schedule-grid` удалена локально и на GitHub). Перед слиянием в ветку докоммичен перевод биллинг-экшенов на `getActionContext()` + `can(role, 'billing')` (семантика owner-only сохранена). Проверки на слитом main: tsc чисто, vitest 118/118, production build ok. Решение основателя: следующий шаг — мини-этап 1.5 (экспорт PDF/PNG), затем этап 2. |
| 2026-06-12 | **Этап 0 завершён.** App shell с сайдбаром, getAppContext, switchOrgAction, пустые состояния /schedule /employees /dashboard, честный лендинг в 3 локалях. Попутно: найден и устранён дубль APP_PREFIXES в корневом middleware.ts (из-за него /schedule отдавал 404) — теперь один источник в lib/supabase/middleware.ts. Тесты: tsc чисто, vitest 72/72, prod build ok. E2E вживую: лендинг ru/uk/en без фейковых цифр; неавторизованный → /login; логин → онбординг → создание организации (RPC) → дашборд; навигация по 3 разделам; роль «Владелец» в сайдбаре; выход → /login. В dev-БД создан тестовый аккаунт claude.e2e@smengo-test.dev + организация «Claude E2E Test Org» — пригодится для этапа 1. |
| 2026-06-12 | **Правка 2 (фидбек основателя): 4 пункта** (та же ветка, коммиты `10ec1cf`…`a5b76f7`). 1) «График светится красным» → в просмотре только красные числа дня и счётчики «НА СМЕНЕ», полные демо-визуалы проблемных колонок (тонировка + dashed-чипы) перенесены в режим «Правка». 2) Дубль «Сотрудники» устранён: табы над гридом убраны, вкладка целиком переехала на /employees (серверная загрузка под RLS, карточки/список/оверлей, поиск в shallow-URL). 3) Тёмная тема: реальная причина «невидимого» заголовка — z-index поповера под липкой шапкой (поднят на z-50); «Пороги» и «Статусы» перестилизованы в демо-язык; добавлен флип якоря поповеров при нехватке места. 4) **Сохраняемый «Вид» грида**: таблица `grid_view_settings` (применена к живой БД; RLS: читают члены, пишут owner/admin/manager; jsonb ≤64KB), право `customize_view`, Zod-схема формата CustomCellConfig демо, конструктор «Визуал» (single/split до 3 секторов, эмодзи/цвета, темы light/dark раздельно, живой предпросмотр, «Сбросить»), персист тумблеров «Отображения» (optimistic + debounce 600мс), применение cardVisuals во всех 3 режимах грида. Новые статусы автоматически появляются в «Визуале»; вся организация и шарящиеся ссылки видят сохранённый вид. Проверки: tsc чисто, vitest 130/130 (+12 новых на card-visual), build зелёный, живой E2E (настройка→reload→сохранилось, обе темы, новый статус в списке). Хвост: flush несохранённого debounce при закрытии вкладки не делается (осознанно); живой логин viewer-ролью не прогонялся. |
| 2026-06-12 | **Правка 1: продуктовый грид приведён к виду демо-грида лендинга 1:1** (ветка `fix/grid-demo-parity`, 6 коммитов). Визуальный слой /schedule переписан по образцу `grid-preview.tsx` (standard): шапка дней (54/44px, бейдж «сегодня», границы недель, проблемные колонки), ячейка сотрудника (Avatar-градиенты без фото, DepartmentPositionCard, цвет отдела по stableHash(id) из палитры демо), чипы статусов всех 3 режимов (work-карточки по окнам смен утро/вечер/ночь, отпуск/больничный с иконками, merge-прогоны V/S c клик-таргетингом по дню, «не назначено» в колонках ниже порога), итоги Вых/Час, строка «НА СМЕНЕ» внизу (верхнее «Покрытие» удалено), тулбар-пилюли с табами DemoTabs, режим «Правка/Готово» (гейтит редактор и dnd), Экспорт-тост, поповер «Отображение», тумблер Проекты/Телеграм, AppleHScrollbar. Архитектура не тронута: TanStack Virtual (высоты строк 44/57/80 сняты с живого демо), TanStack Query, server actions, RLS. i18n ru/uk/en. Проверки: tsc чисто, eslint 0 ошибок, vitest 118/118, живой E2E в превью (все типы чипов, merge ×4, пороги, тёмная тема, лендинг не задет). `npm run build` впоследствии прогнан основной сессией — зелёный (сетевая проблема субагента с Google Fonts не воспроизвелась). Догон `73be014`: попап редактора ячейки приведён к демо (.smengo-pop, чипы статусов без сырых кодов — закрыт пункт полиш-бэклога, пресеты в цветах смен, кольцо активного статуса); живая проверка вкл. сохранение в БД после reload. Открытые хвосты паритета: содержимое поповеров порогов/статусов (alerts-form, status-manager) в старом стиле; dashed-стена «не назначено» на пустых месяцах с порогом — обсудить с основателем. |
